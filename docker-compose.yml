services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rubato-backend
    ports:
      - "5001:5000"
    env_file:
      - .env.docker
    restart: unless-stopped
